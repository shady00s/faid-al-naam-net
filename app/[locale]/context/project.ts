import { createContext } from "react";

const ProjectBackgroundContext = createContext<any> (null)
const ProjectDetailsContext = createContext<any>(null)

export  {ProjectBackgroundContext,ProjectDetailsContext}